package es.eriktorr.katas

class BowlingScoreCalculator {

    companion object {
        val FRAMES_TOTAL = 10
    }

    fun scoreOf(gameLine: String): Int {
//        val frames = framesFrom(gameLine)



//        println("$frames")

        return 0
    }

//    private fun framesFrom(game: String) = game.split("\\s".toRegex(), FRAMES_TOTAL)
//            .mapIndexed { index, it -> Frame(index + 1, rollsFrom(it)) }
//
//    private fun rollsFrom(frame: String): List<Int> {
//        return frame.toCharArray().map { it -> { println("$it"); return ""; } }
//    }

}